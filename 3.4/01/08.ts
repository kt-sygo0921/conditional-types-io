import { User } from './07'

// const newUser1 = {}
// newUser1.age = 20 // age が型に存在しないため コンパイルエラー

// export const newUser2 = {} as User
// newUser2.age = 20

// export const newUser3 = <User>{}
// newUser3.age = 20

// export function someProc(arg: string): number {
//   // 互換性がないためコンパイルエラー
//   return arg as number
// }

// export function someProc(arg: string): number {
//   // any にアップキャストしたうえで、assert しているため コンパイルエラー とならない
//   return arg as any as number
// }
